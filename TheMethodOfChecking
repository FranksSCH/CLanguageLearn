#include <stdio.h>
#include <stdlib.h>
int main() {
    int a;
    a = 0;
    while (a <= 3) {
        a = a + 1;
        printf("%d\n", a);

    }
    printf("%d\n", a);
    while (a >= 1) {
        printf("%d\n", a);
        a = a - 1;
    }
    return 0;
}
